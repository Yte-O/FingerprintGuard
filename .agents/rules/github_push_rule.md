# GitHub Push Rule

**Rule:**
Do NOT automatically push code to GitHub (`git push`) unless the user explicitly asks for it (e.g. "push to github", "推送到 github", "帮我推代码" etc.).
You may `git add` and `git commit` to maintain version history if helpful, but never push to remote repositories without explicit permission.
